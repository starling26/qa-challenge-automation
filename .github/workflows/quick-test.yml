name: 🔄 Quick Test on Push

on:
  push:
    branches: [ main, develop, feature/* ]
    paths:
      - 'qa-challenge-automation/tests/**'
      - 'qa-challenge-automation/playwright.config.ts'
      - 'qa-challenge-automation/package.json'
      - '.github/workflows/**'

env:
  BASE_URL: https://parabank.parasoft.com/parabank

jobs:
  quick-validation:
    name: 🚀 Quick Validation
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./qa-challenge-automation
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: './qa-challenge-automation/package-lock.json'

      - name: 📦 Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: 🎭 Install Playwright browsers and dependencies
        run: |
          npx playwright install chromium
          npx playwright install-deps chromium

      - name: 🧪 Run smoke tests
        run: |
          echo "🚀 Running quick smoke tests on push..."
          npx playwright test \
            --project=chromium \
            --grep="TC01|TC02|TC03" \
            --reporter=line \
            --max-failures=3
        continue-on-error: true

      - name: 📊 Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: quick-test-results
          path: |
            qa-challenge-automation/test-results/
            qa-challenge-automation/playwright-report/
          retention-days: 7

      - name: 📊 Quick status
        run: |
          echo "✅ Quick validation completed"
          echo "📋 Full test suite will run on PR or manual trigger"
